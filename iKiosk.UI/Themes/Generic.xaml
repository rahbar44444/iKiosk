<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:iKiosk.UI.CustomControls">

    <Style TargetType="{x:Type local:IconButton}">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="Black"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="Foreground" Value="Black"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:IconButton}">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10"
                            Padding="{TemplateBinding Padding}">
                        <Grid x:Name="LayoutRoot" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Default Layout: Icon Left -->
                            <Image x:Name="IconElement"
                                   Grid.Row="0" Grid.Column="0"
                                   Source="{Binding IconSource, RelativeSource={RelativeSource TemplatedParent}}"
                                   Width="{Binding IconWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                   Height="{Binding IconHeight, RelativeSource={RelativeSource TemplatedParent}}"
                                   Margin="0,0,10,0"
                                   Stretch="Uniform"
                                   RenderOptions.BitmapScalingMode="HighQuality"/>

                            <TextBlock x:Name="TextElement"
                                       Grid.Row="0" Grid.Column="1"
                                       Text="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}"
                                       FontSize="{TemplateBinding FontSize}"
                                       FontWeight="{TemplateBinding FontWeight}"
                                       VerticalAlignment="Center"
                                       Foreground="{TemplateBinding Foreground}"
                                       TextAlignment="Center"/>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>

                        <!-- Icon at TOP -->
                        <Trigger Property="IconPlacement" Value="Top">
                            <Setter TargetName="IconElement" Property="Grid.Row" Value="0"/>
                            <Setter TargetName="IconElement" Property="Grid.Column" Value="0"/>
                            <Setter TargetName="TextElement" Property="Grid.Row" Value="1"/>
                            <Setter TargetName="TextElement" Property="Grid.Column" Value="0"/>
                            <Setter TargetName="IconElement" Property="Margin" Value="0,0,0,10"/>
                            <Setter TargetName="TextElement" Property="Grid.ColumnSpan" Value="2"/>
                        </Trigger>

                        <!-- Icon at BOTTOM -->
                        <Trigger Property="IconPlacement" Value="Bottom">
                            <Setter TargetName="TextElement" Property="Grid.Row" Value="0"/>
                            <Setter TargetName="IconElement" Property="Grid.Row" Value="1"/>
                            <Setter TargetName="IconElement" Property="Margin" Value="0,10,0,0"/>
                            <Setter TargetName="TextElement" Property="Grid.ColumnSpan" Value="2"/>
                            <Setter TargetName="IconElement" Property="Grid.ColumnSpan" Value="2"/>
                        </Trigger>

                        <!-- Icon at RIGHT -->
                        <Trigger Property="IconPlacement" Value="Right">
                            <Setter TargetName="IconElement" Property="Grid.Column" Value="1"/>
                            <Setter TargetName="TextElement" Property="Grid.Column" Value="0"/>
                            <Setter TargetName="IconElement" Property="Margin" Value="10,0,0,0"/>
                        </Trigger>

                        <!-- Icon at LEFT -->
                        <Trigger Property="IconPlacement" Value="Left">
                            <Setter TargetName="IconElement" Property="Grid.Column" Value="0"/>
                            <Setter TargetName="TextElement" Property="Grid.Column" Value="1"/>
                            <Setter TargetName="IconElement" Property="Margin" Value="0,0,10,0"/>
                        </Trigger>

                        <!-- Hover -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#FFEEE5"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="#E67E22"/>
                            <Setter Property="Foreground" Value="#E67E22"/>
                        </Trigger>

                        <!-- Pressed -->
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#E67E22"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>

                        <!-- Disabled -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
